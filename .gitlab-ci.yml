before_script:
  - php -v
  - apt-get -qq  -y update
  - apt-get -qq  -y install git zip unzip
  - php -r "copy('https://getcomposer.org/installer', 'composer-setup.php');"
  - php composer-setup.php
  - php -r "unlink('composer-setup.php');"

czechDataBox:7.1:
  image: php:7.1
  script:
    - php composer.phar install --no-progress --no-interaction --no-suggest -q
    - vendor/bin/phpstan analyse -c phpstan.neon -l 7 src/
    #- vendor/bin/tester -C tests/php.ini tests/

czechDataBox:7.2:
  image: php:7.2
  script:
    - php composer.phar install --no-progress --no-interaction --no-suggest -q
    - vendor/bin/phpstan analyse -c phpstan.neon -l 7 src/
    #- vendor/bin/tester -C tests/php.ini tests/

czechDataBox:7.3:
  image: php:7.3
  script:
    - php composer.phar install --no-progress --no-interaction --no-suggest -q
    - vendor/bin/phpstan analyse -c phpstan.neon -l 7 src/
    #- vendor/bin/tester -C tests/php.ini tests/
